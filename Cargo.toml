[package]
name = "nskm"
version = "0.2.5"
authors = ["Shiwei Zhang <ylxdzsw@gmail.com>"]
edition = "2021"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true

[dependencies]
libc = { version="0.2", default-features=false }
